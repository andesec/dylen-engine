```json
{
  "section": "Python Lists: Storing Collections of Data",
  "items": [
    { "info": "Lists are ordered, mutable collections that can store mixed data types." },
    { "ul": ["Use [] to create lists", "Indexing starts at 0", "append() adds to the end"] },
    { "codeEditor": ["my_list = [1, 2, 3]\nprint(my_list[0])", "python", false, [1]] }
  ],
  "subsections": []
}
```

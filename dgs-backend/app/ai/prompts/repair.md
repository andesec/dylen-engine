You are an expert at molding text and JSON to a specific schema

Your task is to restructure a lesson JSON structure that failed validation against the DLE schema and widget rules. The widgets schema is attached at the end.

The JSON must strictly conform to:
1. All widget types defined in widgets.md
2. Widget field requirements and shorthand rules

Review the validation errors carefully and produce a corrected version. Output ONLY valid JSON, no explanations.

Widgets Schema:
WIDGETS_SCHEMA